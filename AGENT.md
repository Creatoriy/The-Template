# The-Template - Контекст для AI

Данный файл предназначен для ИИ помощника, который будет помогать с разработкой скрипта и доработкой заготовки.

## 🎯 Суть проекта

**The-Template** - это образовательная заготовка кода для автоматизации работы в браузере и Web3. Создан для студентов курса https://t.me/maxzarev для обучения автоматизации без погружения в низкоуровневую реализацию.

## 👥 Целевая аудитория

Студенты курса по автоматизации действий в браузере и Web3, которые учатся писать скрипты для работы с:

- Множественными профилями в ADS Power
- Metamask и Web3 кошельками
- Блокчейн транзакциями (EVM сети)
- Централизованными биржами (OKX, Binance)
- Excel отчётностью

## 🏗️ Архитектура (кратко)

### Основные модули:

- **Bot** - центральный класс, объединяет все модули (context manager)
- **Ads** - управление браузером ADS Power через Playwright
- **Metamask** - автоматизация работы с Metamask расширением
- **Onchain** - работа с блокчейном через Web3.py (балансы, транзакции, gas)
- **Excel** - чтение/запись данных и статистики
- **Exchanges** - вывод средств с OKX и Binance

### Ключевые модели:

- **Account** - данные профиля (номер, адрес, приватник, seed, прокси, пароль)
- **Chain** - параметры блокчейн сети (rpc, chain_id, названия для бирж/metamask)
- **Token** - параметры ERC20 токена (адрес, decimals, chain)
- **Amount** - работа с суммами токенов (wei ↔ human-readable)

### Хранилища:

- **Chains** - статический класс со всеми сетями (Ethereum, Arbitrum, BSC и т.д.)
- **Tokens** - статический класс со всеми токенами (USDT, USDC по сетям)

## 📂 Структура данных

Данные загружаются из `config/data/accounts.xlsx` (предпочтительно) или txt файлов. Настройки в `config/settings.py`, приватные ключи API в `.env`.

## 🎓 Задачи документирования (текущий фокус)

Сейчас проект активно документируется для студентов:

- Добавление подробных docstrings с примерами
- Комментирование сложной логики
- Расширение README с руководствами по каждому модулю
- План документирования в `DOCUMENTATION_PLAN.md`

## ⚠️ Важные правила при работе

1. **НЕ редактируй код без прямого запроса** - только предлагай изменения
2. **НЕ создавай .env** - он скрыт, проси пользователя
3. **Думай на английском, отвечай на русском**
4. **Код для обучения** - приоритет на читаемость и понятность
5. **Комментарии только где нужно** - код должен быть самодокументируемым

## 🔑 Типичный workflow студента

```python
# Студент создаёт функцию активности
def activity(bot: Bot):
    bot.ads.open_url('dapp.com')           # открыть dApp
    bot.metamask.connect(button_locator)    # подключить кошелёк
    # ... логика работы с dApp через ads
    bot.metamask.send_tx(tx_button)        # подтвердить транзакцию
    bot.excel.increase_counter('Swaps')    # записать статистику
```
